# SOP：DataWind 看板数据同步到飞书表格（通用流程）

写给完全没有上下文的新对话看。这是一份**可复用的标准操作流程**，适用于任意 DataWind 看板 + 任意飞书表格的组合，不绑定具体项目。如果你要接的是"把 X 看板的数据同步到 Y 飞书表格"这类任务，照此流程做。

本文档不包含任何本项目专用的具体 token、sheet_id、URL——那些记录在 `HANDOFF.md` 里。这里只讲方法和坑。

---

## 0. 先决条件检查

开始前确认以下工具是否可用，缺什么装什么：

```bash
# Node.js（用于 lark-cli）
node --version

# Python 3（用于抓取和写入脚本）
python3 --version

# Python 依赖：websocket-client（CDP 通信用）
python3 -c "import websocket; print(websocket.__version__)"
# 没有就装：pip3 install websocket-client（不要装 requests，见坑 #1）

# 飞书 CLI（不需要全局安装，用 npx 调用即可）
npx --yes @larksuite/cli@latest --version
```

**关于网络环境**：如果 DataWind 是内网系统（需要 VPN 才能访问），而飞书/GitHub 走的是另一套代理，先搞清楚这两套网络能不能同时连接。如果不能同时连（本项目就是这种情况，FortiVPN 和 Clash 互斥），整个流程必须设计成两个独立阶段，中间用本地文件传递数据，不能设计成一个脚本里同时做"抓取+写入"。这是决定整体架构的关键前提，先确认清楚再往下走。

---

## 1. 飞书授权（一次性，除非过期）

```bash
cd <项目目录>
npx --yes @larksuite/cli@latest auth status
```

如果显示未登录或 `user: missing`，走以下两步：

```bash
# 第一步：配置飞书 App（会输出一个链接/二维码，需要企业管理员或自己扫码确认）
npx --yes @larksuite/cli@latest config init --new

# 第二步：登录授权（会输出一个设备验证链接，浏览器打开并登录）
npx --yes @larksuite/cli@latest auth login --recommend
```

这两步都是**阻塞式命令**，会一直等到用户完成浏览器操作才返回。建议用后台进程方式跑（比如用支持后台任务的终端工具），这样才能持续读取输出、捕获链接。

**验证成功的标志**：`auth status` 返回 `"identity": "user"`，并显示用户名和 token 有效期。

### 避坑：不要混淆"应用配置链接"和"用户登录链接"
- `config init` 输出的链接形如 `https://open.feishu.cn/page/cli?user_code=XXXX-XXXX`，这是给**企业管理员**批准应用权限的。
- `auth login` 输出的链接形如 `https://accounts.larksuite.com/oauth/v1/device/verify?...&user_code=XXXX-XXXX`，这是给**你自己**登录授权的。
- 两者的验证码都是一次性、几分钟内过期。如果隔了很久才点击，会报"授权失败"或"scopes未被授予"，需要重新执行对应命令生成新链接，不能重复用旧链接。

---

## 2. 定位并核实目标飞书表格

拿到用户给的飞书链接后，先确认它是什么类型、能不能访问、能不能写：

```bash
# 如果是 wiki 链接（形如 https://xxx.larksuite.com/wiki/<token>）
npx --yes @larksuite/cli@latest wiki +node-get --node-token "<完整wiki链接或token>"
```

返回结果里关注：
- `obj_type`: 应该是 `sheet`（如果是 `doc`/`docx` 说明链接给错了，那是文档不是表格）
- `obj_token`: 这是真正用于后续表格操作的 **spreadsheet_token**，跟 wiki 链接里的 token 不是一回事，务必用 `obj_token`
- `title`: 核对一下标题跟用户说的是否一致，尤其注意"副本"（copy）这种情况——用户给的链接可能是原表的一个副本，不是原表本身，必须跟用户确认清楚**最终要写入的是哪一个**（原表还是副本），一旦搞混会写错文件。

```bash
# 用 obj_token 查看这个表格里所有的子表（tab）
npx --yes @larksuite/cli@latest sheets +workbook-info --spreadsheet-token "<obj_token>"
```

返回的 `sheets` 数组里每个对象有 `sheet_id` 和 `sheet_name`，找到用户说的目标 tab 名称，记下它的 `sheet_id`（后续所有读写操作都用 `sheet_id`，不是 `sheet_name`，虽然两者都能用但 `sheet_id` 更稳定不受改名影响）。

### 避坑：先读懂现有表格结构，不要瞎猜列/行含义
```bash
npx --yes @larksuite/cli@latest sheets +csv-get \
  --spreadsheet-token "<obj_token>" \
  --sheet-id "<sheet_id>" \
  --range "A1:Z50" \
  --output-path "artifacts/template-preview.json"
```
读出来之后，人工看一遍（或用脚本分析）表头是什么、每个任务占几行、有没有空行分隔、流程名称和任务名称填在哪些列、哪些行是模板占位（值为空等着填）。**不要假设列结构，每个用户的表格设计可能完全不同**，必须先读一遍原始内容再动手写。

重点确认这件事：**表格里已经填好的"业务实体名称"（比如任务名、流程名、活动名等）就是本次要追踪同步的范围**，不要反过来用 DataWind 那边的全量数据去决定写哪些行——通常 DataWind 一次返回的数据远比飞书表格里要追踪的对象多得多。

---

## 3. 定位 DataWind 的真实数据接口

DataWind（以及类似的 BI 工具）大多数图表用 canvas 渲染，网页 DOM 里读不到数据，必须通过浏览器网络请求层去截获。

### 3.1 启动带调试端口的独立 Chrome

写一个类似这样的启动脚本（关键点见下方避坑）：

```bash
#!/bin/bash
PORT=9222
PROFILE_DIR="/tmp/chrome-cdp-profile"
CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"   # macOS 路径，其他系统要改
TARGET_URL="${1:-<默认看板URL>}"

"$CHROME" \
  --remote-debugging-port="${PORT}" \
  --remote-allow-origins='*' \
  --user-data-dir="$PROFILE_DIR" \
  --new-window \
  "$TARGET_URL" &

sleep 3
curl -fsS "http://127.0.0.1:${PORT}/json/version" && echo "CDP ready"
```

用独立的 `--user-data-dir`，这样不会影响用户日常使用的 Chrome 窗口/登录态，用户可以在这个新窗口里单独登录目标系统。

### 避坑：Shell 脚本里变量后面紧跟非空格字符（尤其是中文全角符号）会报错
写 `echo "端口 $PORT）"`（中文右括号紧贴变量名）会被 bash 解析成变量名 `PORT）`，导致 `unbound variable` 报错。永远写成 `${PORT}` 显式加大括号，尤其是脚本里混用中文字符的时候。

### 3.2 监听网络请求

写一个基于 `websocket-client` 的 CDP 监听脚本，核心逻辑：

1. 通过 `http://127.0.0.1:9222/json` 拿到当前打开的 tab 列表，找到 URL 包含目标看板标识的那个 tab，取它的 `webSocketDebuggerUrl`。
2. 建立 WebSocket 连接后依次发送：`Network.enable` → `Network.setCacheDisabled` → `Page.enable`。
3. **不要自动刷新页面**（除非你确定第一次进入就能拿到想要的数据），改为提示用户"现在开始监听，请手动在浏览器里操作并刷新"，然后在固定的时间窗口内（比如 60-120 秒）收集所有 `Network.requestWillBeSent` / `Network.responseReceived` / `Network.loadingFailed` 事件。
4. 时间窗口结束后，对每个 `type` 为 `XHR` 或 `Fetch` 的响应调用 `Network.getResponseBody` 拿到实际响应内容（`Network.responseReceived` 事件本身不带 body，需要单独用 `requestId` 再查询一次）。
5. 把结果（事件列表 + 响应体列表）存到本地文件，**文件名一定要带时间戳**，见坑 #2。

### 避坑：抓取文件不要用固定文件名，永远互相覆盖
如果每次抓取都写入同一个文件名（比如 `network-events.json`），一旦某次抓取失败或没抓到想要的数据（比如忘了手动刷新页面），旧的有效数据就会被空结果覆盖，之前的抓取全部白费。**每次抓取生成的文件名都带上时间戳**（如 `{YYYYMMDDTHHMMSS}-events.json`），永久保留每次抓取的原始记录，方便随时回溯对比。

### 避坑：不要用"最近N天/最近一周"这类相对时间筛选去抓周期性数据
如果目标是抓某个已经结束的固定周期（比如"上周"、"某月某周"），必须在 BI 工具界面手动切换成**绝对日期范围**筛选，抓取后检查请求体里的筛选条件是 `绝对时间`（比如 `between: [开始时间, 结束时间]`）还是 `相对时间`（比如 `最近N天`）。相对时间会随着当前日期变化而变化，抓到的可能是还没结束的当前周期，数据不完整、不稳定，不能用于历史周期汇总。

### 避坑：留意分页/行数上限，光看"返回成功"不代表数据完整
很多 BI 查询接口有单次返回上限（比如1000行），响应里往往会有一个字段提示"实际总量"（类似 `total` / `atLeast` / `hasMore`）。**每次抓取后先检查这个字段**，如果提示的总量远超过返回的行数，说明被截断了。如果你要追踪的具体任务/记录恰好落在被截断的部分之外，会导致"明明数据存在，但抓取结果里找不到"的假象，容易误判为"数据不存在"或"名称不一致"。

**排查方法**：先怀疑是不是被截断，而不是怀疑数据本身有问题或用户看错了。解决办法是在 BI 界面上用筛选器把范围收窄到只包含你要的那批任务/记录（比如按单个流程/分类筛选），单独刷新抓取一次，这样返回的数据量会小很多，基本能覆盖到目标内容。**用户在界面上实际看到的数据，才是最终判断依据**——如果抓取结果和用户界面描述不一致，优先怀疑抓取端被截断或筛选条件不对，不要轻易断定是数据源本身的问题。

### 避坑：字段的内部 ID 不是固定不变的，别硬编码
BI 系统内部经常用一堆数字/哈希 ID 代表每个指标字段，同一个中文指标名在不同请求里，对应的内部 ID 可能不同。正确做法：**每次都从当次响应自带的"字段ID→显示名称"映射表里动态查找**，不要把上次抓取时见到的 ID 直接硬编码到下次的脚本里复用。同理，别用中文显示名称做精确字符串匹配去取值，容易因为不可见空格、全角/半角差异等问题匹配失败；应该先用名称从映射表反查出当次有效的字段 ID，再用 ID 取值。

---

## 4. 匹配 DataWind 数据 ↔ 飞书表格模板

拿到 DataWind 响应后，别急着往飞书写，先做匹配校验：

1. 从飞书表格模板里提取所有"业务实体标识列"的值（比如"流程名称+任务名称"的组合，具体看你的表格设计用几列做唯一标识）。
2. 从 DataWind 响应的每条记录里，同样取出对应字段的值。
3. 用**完整的组合键**（不是单一字段）去匹配，比如同时用"流程名称+任务名称"两个字段做联合匹配，不要只用其中一个字段（可能会有一对多的情况，是正常业务逻辑，不要当成异常数据去重）。
4. 打印一份匹配报告：多少行能精确匹配、多少行匹配不到。**匹配不到的先不要瞎猜或强行替换成相似的记录**，如果数量少（几行），列出来跟用户确认；如果数量多，先怀疑是不是抓取范围/筛选条件不对（比如筛选周期错了、被截断了），重新抓一次。

### 避坑：让用户先确认追踪范围的"字面值"以谁为准
如果飞书表格和 DataWind 里同一个业务对象的命名不完全一致（比如版本号、日期后缀不同），不要自己猜测该用哪个，直接问用户"以飞书表格里当前填写的名称为准"还是"以 DataWind 里看到的名称为准"，并且**明确后就不要中途更改**，全流程统一。

---

## 5. 写入飞书表格

### 避坑：飞书 CLI 的批量写入模式可能在本机环境不稳定
用 `sheets +cells-set --writes '<JSON数组>'` 一次性提交多个不连续区域的写入，在某些本地环境下会导致底层进程被系统强制终止（比如 `SIGKILL`），且没有明确的错误信息说明原因。**每次批量写入前，先用 `--dry-run` 跑一遍确认请求本身没问题**；如果正式提交（去掉 `--dry-run`）时进程异常终止，**不要立刻重试**，先回读目标单元格确认是否已经写入了部分数据，避免重复写入或遗漏。

如果反复遇到批量写入被终止的问题，改用**逐行单独调用**的方式：一次只写一个连续区域（比如一行的 B 到 H 列），参数量小很多，稳定性显著提升：

```bash
npx --yes @larksuite/cli@latest sheets +cells-set \
  --spreadsheet-token "<token>" \
  --sheet-id "<sheet_id>" \
  --range "B<行号>:H<行号>" \
  --cells '[[{"value":"..."},{"value":123},...]]'
```

写一个 Python 脚本封装这个逐行调用逻辑（读取模板 → 匹配 DataWind 数据 → 遍历逐行调用），比手写一长串 JSON 命令行参数更可控、更容易加日志和异常处理。

### 避坑：不要用手写的大段 JSON 文件做写入请求的中间产物
如果用文件编辑工具手写/拼接大段 JSON 字符串作为批量写入的请求体，容易遇到：文件被写入了不相关的内容、编辑器和写入工具之间产生版本冲突、文件保存后变成空文件等问题。**改成用脚本在内存里直接构建请求体并直接调用 CLI**，完全不落地这种大 JSON 中间文件，从根本上避免这类冲突。

### 避坑：写入前先确认是不是要新建 tab，新建方式用"复制"而不是"手搭空表"
如果本次同步需要一个全新的目标 tab（比如按周期滚动新建），且已有同结构的旧 tab 可以参考，优先用"复制现有 tab"的方式创建新 tab（保留列宽、合并单元格、格式），再用"清空内容保留格式"的方式清掉旧数据，而不是新建一个空表再手动搭建格式。这样能保证新旧 tab 结构完全一致，后续写入的行列对齐不会出错。

### 写入后必须回读验证
每次写入完成（无论批量还是逐行）之后，都要用 `+csv-get` 重新读一遍刚写的区域，确认数值、格式（百分比/整数/小数位）符合预期，而不是只看写入命令返回的 `"ok": true` 就认为万事大吉——返回成功只代表 API 调用成功，不代表写入的数值逻辑正确。

---

## 6. 处理网络环境切换（如果适用）

如果 DataWind 和飞书处于互斥的网络环境（类似本项目 FortiVPN vs Clash 的情况），整体执行顺序固定为：

1. **网络环境A（能访问 DataWind）**：只做抓取，产出本地时间戳文件，不涉及任何飞书调用。
2. **切换到网络环境B（能访问飞书）**：只做匹配和写入，读取上一步产出的本地文件，不涉及任何 DataWind 调用。

两个阶段之间用文件系统传递数据，脚本设计上要求"单阶段脚本只依赖一种网络环境"，这样切换网络时不会让脚本莫名其妙失败，方便用户按自己的网络状态选择要跑哪一段。

### 避坑：网络切换后不要立刻假设已经生效
FortiVPN/代理软件切换不是瞬间完成的，尤其是刚断开一个连接、启动另一个的时候，DNS 解析可能已经变化但实际连接还没打通（比如 `curl` 会遇到 DNS 能解析出 IP，但连接本身超时/失败）。**每次网络切换后，先用一个简单的连通性检测命令确认目标服务真的可达**，再执行正式的抓取或写入操作：

```bash
curl -s -o /dev/null -w "%{http_code}\n" --connect-timeout 5 https://<目标服务域名>
```
返回 `200`（或其他非 `000` 的正常HTTP状态码）才说明网络确实通了，`000` 或长时间无响应说明还没切换完成或存在拦截。

### 避坑：自动化的 Git commit/push hook 在网络切换期间会报错，这是正常现象，不是任务失败
如果项目配置了自动提交代码的 hook，在访问不到 GitHub 的网络环境下会报 SSL/连接错误。**应该提前把 hook 设计成"本地 commit 失败才算真正失败，push 失败只警告不报错"**，这样即使处于隔离的内网环境里工作，本地版本历史依然能正常积累，等网络恢复后再手动或自动补推。

---

## 7. 整体检查清单（开工前过一遍）

- [ ] 飞书 CLI 登录状态有效（`auth status` 确认）
- [ ] 已明确目标飞书表格是原表还是副本，拿到正确的 `spreadsheet_token`
- [ ] 已读一遍目标 tab 的现有内容，理解列结构、行结构、模板占位规则
- [ ] 已明确"以谁的命名为准"（飞书表格现有内容 vs DataWind 数据本身）
- [ ] 已确认目标周期/范围要用绝对时间筛选，不是相对时间
- [ ] 抓取脚本文件名带时间戳，不会互相覆盖
- [ ] 抓取后检查过是否有分页截断风险
- [ ] 写入前跑过 dry-run
- [ ] 写入后做了回读验证
- [ ] 如果涉及网络环境切换，脚本按阶段拆分，且每次切换后做了连通性检测

---

## 8. 建议的项目文件结构（参考）

```
项目根目录/
├── scripts/
│   ├── open-chrome.sh              # 启动带CDP的独立Chrome
│   ├── capture-<数据源>-requests.py # CDP网络监听脚本，输出到 artifacts/
│   └── write-<目标>.py              # 读取抓取结果+飞书模板，逐行写入
├── artifacts/                       # 所有抓取结果和临时文件，加入.gitignore
├── HANDOFF.md                       # 项目专属的交接文档（当前进度、未解决问题、专属坑）
└── SOP.md                           # 本文档，通用方法论，换个数据源/表格也能复用
```

`artifacts/` 目录务必加入 `.gitignore`，因为抓取结果通常包含内部业务数据，不适合提交到代码仓库。
