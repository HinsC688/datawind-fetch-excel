{
  "enabled": true,
  "name": "KYC 週度數據同步",
  "description": "每周执行 DataWind → 飞书 KYC 整體觸達數據 的同步。因为 FortiVPN 和 Clash 互斥，流程分两段，需要人工切换网络。触发后我会引导你走完全程。",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "执行 KYC 週度數據同步。请先读 HANDOFF.md 的第 7A 节（KYC 整體觸達數據）了解完整背景、业务口径和已知的坑，然后按下面的流程走。不要跳过检查步骤。\n\n**⚠️ 最先做这一步：检查 HANDOFF.md 里有没有\"🔴 未完成事项\"小节。**\n如果有，先处理它再做本周期的常规同步；处理完把该小节从 HANDOFF.md 删掉（或标记为已完成）。\n截至 2026-08-18 有一项待办：把飞书里的 `0812-0817` 覆盖成 `0812-0818`（详情见该小节）。\n这一项的特殊之处是**覆盖已有行、不插入新行、不补数字格式**，写入起始行分别是 push 117 / 邮件 112 / 弹窗 83。\n注意它就是 2026-08-19 周三这一轮的正常周期，不是额外的一期，处理完当天就不用再同步别的周期了。\n\n**第0步：确定周期（规则已固定，直接算，不用问用户）**\n周期定义：**周三 → 下周二，固定7天**。标签格式 `MMDD-MMDD`（如 `0812-0818`）。\n在周三运行时，同步的是\"刚刚结束的那一期\"，即上周三到昨天(周二)。\n例：周三 2026-08-19 运行 → 周期是 2026-08-12(周三) ~ 2026-08-18(周二)，标签 `0812-0818`。\n\n**重要校验**：算完后必须确认 (结束日 - 开始日 + 1) == 7、开始日是周三、结束日是周二。\n不满足就说明算错了或者运行日不是周三——此时要停下来问用户，不要将就抓一个不完整的周期。\n历史上 `0812-0817` 这一期就是因为提前在周二跑，只抓了6天，跟其他7天的周期不可比。\n**绝不要抓包含\"今天\"的范围**，当天数据不完整。\n\n同时用 `npx --yes @larksuite/cli@latest auth status` 确认飞书登录有效（refresh token 只有7天有效期，过期要 `auth login --recommend`）。\n\n**第1步：核对追踪名单（这一步必须人工确认，不能跳过）**\n跑 `python3 scripts/detect-write-rows.py` 看三个 tab 的最新周期和最后一行。\n然后读出飞书三个 tab 最新一期的流程名称，跟上一版 `config/touch-details-*.txt` 对比。\n重点：**弹窗的实验名称几乎每1-2周就换一轮**（历史上 0706→0715→0722→0727→0806→0813），push 和邮件那 43 个名称比较稳定。\n把差异列给用户确认，确认后生成新的 `config/touch-details-{周期}.txt` 和 `config/write-plan-{周期}.json`（可以用 `detect-write-rows.py --update-config` 自动填行号）。\n\n**第2步：取数（需要 FortiVPN）**\n提醒用户切到 FortiVPN，切换后会断线。用 `curl -s -o /dev/null -w \"%{http_code}\" --connect-timeout 5 https://datawind.xiaoxiame.com` 确认连通（返回000表示没连上）。\n优先用 API 重放方式（不需要手工点界面）：\n  bash scripts/open-chrome.sh \"https://datawind.xiaoxiame.com/bi/pages/dashboard/40450?appId=2&sheetId=14003\"\n  # 用户在弹出的Chrome里确认已登录DataWind\n  python3 scripts/fetch-datawind.py --start {开始日期} --end {结束日期} --details config/touch-details-{周期}.txt\n如果 API 重放失败（比如模板过期、页面结构变了），退回手工抓取方式：先启动 `capture-datawind-requests.py --dashboard-id 40450 --no-reload --seconds 180`，再去界面操作（顺序不能反，见坑#15）。\n\n**第3步：校验抓取结果**\npython3 scripts/check-capture.py artifacts/{时间戳}-response-bodies.json\n必须确认：有 vizQuery/query 请求、日期是 between 绝对范围、未触及 limit、唯一键不重复。\n\n**第4步：写入（需要 Clash）**\n提醒用户切回 Clash 并确认飞书可访问。\n先 dry-run：`python3 scripts/write-kyc-weekly.py --tab all`，把预览数值跟上一周期做量级对比，异常要停下来问用户。\n然后对每个 tab：先用 `+dim-insert --position {插入位置} --count {行数+1} --inherit-style before` 插入带格式的行（不要直接往空行写，否则百分比格式丢失，见坑#19），再 `write-kyc-weekly.py --tab {tab} --start-row {起始行} --apply`。\n**如果本次是覆盖已有周期的数据**（比如修正一个不完整的周期），则跳过 `+dim-insert`，直接用 `--start-row` 指向已有行的起始位置覆盖写入；那些行的数字格式已经是对的，不用再补 number-format。\n\n**第5步：回读验证**\n用 `+csv-get` 读回写入区域，确认数值正确、百分比显示成 `2.48%` 而不是 `0.0248`、整数有千位分隔符。\n\n**第6步：更新文档**\n把本次周期、写入行号、遇到的新问题追加到 HANDOFF.md 第7A节。\n\n全程遵守：写入前必须 dry-run；破坏性操作（清空/删除）要先问用户；发现数据异常或名单对不上时停下来问，不要自己猜。"
  }
}
