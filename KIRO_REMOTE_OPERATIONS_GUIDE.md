# Kiro 远端操作通用指南

> 面向希望从另一台电脑远程操控一台常驻 Mac/Linux 主机，并在主机上运行 Kiro CLI、项目脚本和浏览器自动化的用户。
>
> 本文不包含任何项目 token、公司域名、订阅链接、账号、内网网段或业务数据。请用自己的值替换示例中的 `<占位符>`。

## 1. 先理解架构：远端设备负责控制，宿主机负责执行

远端操作的关键不是把项目搬到手机或另一台电脑，而是让**宿主机始终是唯一执行面**：项目文件、Kiro CLI 登录态、浏览器会话、CLI 授权、VPN/代理和本地脚本都留在宿主机上；远端设备只通过私有 SSH 通道发出指令、审阅输出和确认高风险动作。

```text
远端设备
  │  私有管理通道（Tailscale + SSH）
  ▼
常驻宿主机（Mac/Linux）
  ├─ 项目工作目录与本地脚本
  ├─ kiro-cli 与 Kiro 云端
  ├─ 浏览器 / CDP 登录会话（如需）
  ├─ 企业 VPN（如 DataWind 等内网数据源需要）
  ├─ 代理/TUN（仅在实际需要时）
  └─ 目标 SaaS CLI/API（如飞书、GitHub）
```

这样做的好处：

- 远端设备断线、换设备或电量耗尽，不会丢失宿主机上的文件和已产生的 artifacts。
- 数据源请求仍从已经授权、已经登录的宿主机发出，不需要把登录态复制到远端设备。
- 可以把“抓取、校验、写入、回读”留在同一台机器上，降低环境差异。

## 2. 需要准备什么

### 2.1 宿主机（执行面）

宿主机应满足：

- 长期开机，且不因休眠而中断网络与 SSH；
- 已安装项目所需运行环境，例如 Python、Node.js、目标 SaaS CLI；
- 已完成 `kiro-cli` 登录，并能在本机运行；
- 已完成目标系统授权，例如飞书 CLI 登录、浏览器登录、企业 VPN 登录；
- 已启用 SSH 远程登录，并限制可登录用户；
- 已安装 Tailscale 并加入自己的 tailnet；
- 项目位于固定路径，且脚本尽量只使用项目内相对路径；
- 如自动化依赖浏览器，浏览器的 CDP 调试端口、专用 profile 和登录态由宿主机维护。

### 2.2 远端设备（控制面）

远端设备应满足：

- 已安装 Tailscale，并加入同一 tailnet；
- 有可靠 SSH 客户端和可用键盘；
- 能通过 Tailscale 名称或 Tailscale IP 连接宿主机；
- 不保存宿主机的业务 token、浏览器 cookie 或 VPN 凭据。

> 平板和笔记本更适合审阅长输出、处理终端交互；手机更适合短命令、状态检查和人工确认。无论设备类型，执行仍在宿主机上完成。

### 2.3 安全基线

- 只通过 Tailscale 私网访问 SSH；**不要**为了远程方便在公网开放 22 端口，也不要做路由器端口转发。
- 使用 SSH 密钥或 Tailscale SSH 身份策略，禁用不必要的密码登录和共享账号。
- 只给远端用户最低必要权限；不要把 root/sudo 当作日常执行身份。
- 不要把 `.env`、私钥、cookie、VPN 配置、SaaS token 写入项目文档、聊天记录或 Git 仓库。
- 对写入数据、删除数据、发布、提交等动作保留人工确认节点。

## 3. 网络决策：先看操作者真实所在地，再决定是否需要代理

不要默认“企业 VPN 和代理必然互斥”，也不要默认“必须开启代理”。正确做法是先测试实际连通性。

| 条件 | 推荐网络策略 |
|---|---|
| 操作者真实位于中国大陆，Kiro CLI / 目标 SaaS / GitHub 等公网服务需要代理 | 在宿主机配置代理的 TUN/虚拟网卡模式，并让它与企业 VPN 共存；分别验证内网数据源、Kiro、SaaS CLI 与 Tailscale。 |
| 操作者真实位于其他国家或地区，Kiro CLI 和公网服务可直接访问 | 不要为了本项目额外开启代理；只连接企业 VPN 访问内网数据源即可，不存在代理与 VPN 的共存问题。 |
| 代理与企业 VPN 无法稳定共存 | 使用“两阶段回退”：VPN 环境只抓取并保存本地 artifacts；可访问 SaaS/Kiro 的网络环境再做校验和写入。 |

### 3.1 代理 TUN 与企业 VPN 共存时的原则

如果确实需要同时开启企业 VPN 和代理 TUN：

1. TUN 用于接管需要代理的命令行/公网流量，例如 Kiro CLI。
2. 企业 VPN 保留公司内网和数据源路由。
3. Tailscale、局域网和企业私网流量应按实际网络策略绕过代理 TUN。
4. 不要硬编码 macOS 的 `utun<N>` 接口名；VPN/TUN 重连后编号会变化。
5. 不要仅凭“网页能打开”判断配置正确，必须分别测试所有实际依赖路径。
6. 若 TUN 使用 Fake-IP/DNS 劫持，内部域名可能需要按客户端规则改为直连或排除 Fake-IP；否则域名解析成功也可能在 TLS/路由阶段失败。

> 私网 CIDR、内网域名、DNS 服务器和具体绕过规则必须以组织网络管理员或实际路由表为准。不要直接复制其他人的网段到生产配置。

## 4. 最小可用的远端登录流程

在远端设备上：

```bash
# 使用自己的 Tailscale 主机名或 IP；不要使用公网地址。
ssh <宿主机用户>@<tailscale-hostname-or-ip>

# 进入项目目录。
cd "<项目绝对路径>"

# 启动 Kiro CLI。
kiro-cli
```

进入 Kiro CLI 后，建议先确认当前可用模型和身份，再开始任务：

```text
/model
```

若项目维护了可复用 prompt，可通过该客户端实际支持的形式调用，例如：

```text
/<prompt-name>
# 或
@<prompt-name>
```

不同 Kiro CLI 版本对 prompt 的呈现方式可能不同；以 `/help` 或 `/prompts` 的实际输出为准。

## 5. 浏览器/CDP 自动化的特殊前提

很多内网 BI、数据平台或人工登录的网站无法通过纯 API 长期使用，常见方案是在宿主机保留一个独立的 Chrome/CDP profile：

1. 宿主机启动带远程调试端口的浏览器；
2. 用户首次在宿主机图形界面完成正常登录；
3. 本地脚本通过 `127.0.0.1:<CDP端口>` 找到已登录页面；
4. 脚本在该浏览器会话上下文中发起查询，浏览器自动携带会话 cookie；
5. 远端 SSH 只负责触发脚本和查看结果。

这不等于绕过登录。浏览器会话过期、多因素认证要求重新验证、或页面要求人工操作时，必须通过宿主机图形界面或经批准的远程桌面重新登录。不要提取、导出或在远端设备复制浏览器 cookie。

## 6. 每次远端任务前的验证矩阵

在宿主机、项目目录中逐项执行；所有依赖均通过后再开始正式抓取或写入。

```bash
# 1) 当前目录与项目文件
pwd

# 2) Kiro CLI 身份（命令按实际安装名称调整）
kiro-cli whoami

# 3) Kiro 公网连通性
curl -s -o /dev/null -w "Kiro HTTP %{http_code}\n" \
  --connect-timeout 8 https://kiro.dev

# 4) 内网数据源连通性（替换为真实数据源域名）
curl -s -o /dev/null -w "Data source HTTP %{http_code}\n" \
  --connect-timeout 8 https://<internal-data-source>

# 5) 目标 SaaS CLI 的授权状态（替换为真实命令）
<saas-cli> auth status

# 6) 若使用 CDP，检查本地调试端口是否可达
curl -fsS http://127.0.0.1:<CDP端口>/json/version
```

建议把验证结果记入本次任务日志。若任何一项失败：**不要**先执行正式写入再排查；先恢复网络、授权或浏览器登录态。

## 7. 推荐执行顺序

1. **计算范围**：确认周期、时区、截止时间和“不包含今天”等业务边界。
2. **确认网络**：按第 3 节和第 6 节验证数据源、Kiro、SaaS 和 Tailscale。
3. **确认追踪范围**：从目标表或策略配置读取当前名单，要求用户确认变更。
4. **抓取**：优先使用已验证的浏览器会话/API 重放；保存带时间戳的原始 artifacts。
5. **校验**：检查日期筛选、行数上限、分页截断、字段映射、唯一键和异常量级。
6. **dry-run**：先生成写入预览，不改远端数据。
7. **人工确认**：让用户确认写入范围、行号和核心数值。
8. **正式写入**：使用已验证的最小写入粒度；避免把大量不连续写入压成单个命令。
9. **回读验证**：读取刚写入的区域，核对值、格式、合并单元格和相邻区域。
10. **记录**：更新交接文档，包括周期、来源文件、写入范围、异常、网络状态和下一期计划。

远端操作只改变“谁发出命令”，不应取消上述校验和人工确认。

## 8. 无法共存时的两阶段回退

当企业 VPN 与代理/TUN 无法同时稳定满足需求时，不要强行把全流程串成一个长脚本：

```text
阶段 A：企业 VPN / 内网环境
  抓取数据 → 校验原始响应 → 保存本地 artifacts（带时间戳）

阶段 B：Kiro / SaaS 可访问环境
  读取本地 artifacts → 匹配 → dry-run → 人工确认 → 写入 → 回读
```

这种拆分使网络切换、远端设备断线或 Kiro CLI 暂时不可用时，已抓到的原始数据不会丢失。

## 9. 常见故障与排查方向

| 现象 | 优先检查 |
|---|---|
| SSH 断开或无法连接宿主机 | Tailscale 登录状态、设备在线状态、SSH 服务、ACL/SSH 策略；不要改用公网端口转发。 |
| kiro-cli 显示模型不可用或无法请求 | 当前模型选择、Kiro 登录身份、代理/TUN 是否实际接管 CLI 流量、Kiro 连通性；不要只测试浏览器。 |
| 内网数据源 DNS 正常但 HTTPS/TLS 失败 | 企业 VPN 路由、代理 TUN 的 Fake-IP/DNS 劫持、内部域名直连规则、企业 DNS。 |
| 数据抓取脚本找不到浏览器页面 | CDP 浏览器未启动、调试端口不通、页面未登录、profile 被清理或登录态过期。 |
| 写入命令成功但表格展示异常 | 回读实际单元格；检查数字格式、百分比、小数/文本类型、合并单元格和插行后的行号。 |
| 远端会话中断 | 确认宿主机仍在执行；重新 SSH 后检查 artifacts 和日志，从最近的确认节点恢复，不要盲目重复写入。 |

## 10. 上线前检查清单

- [ ] 宿主机常驻、可唤醒、SSH 可用。
- [ ] 宿主机和远端设备都在同一 Tailscale tailnet，且没有公网暴露 SSH。
- [ ] Kiro CLI 与目标 SaaS CLI 已在宿主机授权。
- [ ] 已按真实所在地决定是否需要代理；如果需要，企业 VPN 与 TUN 已逐项验证。
- [ ] 数据源、Kiro、SaaS、Tailscale 和 CDP（如需）均通过预检。
- [ ] 已确认浏览器登录态与重新登录方案。
- [ ] 抓取结果以时间戳保存，并在写入前完成校验和 dry-run。
- [ ] 正式写入仍需要人工确认，写后必须回读。
- [ ] 已准备无法共存时的两阶段回退方案。

## 参考资料

- [Tailscale SSH](https://tailscale.com/docs/features/tailscale-ssh)
- [Tailscale iOS 安装说明](https://tailscale.com/kb/1020/install-ios)
- [Mihomo TUN 配置](https://wiki.metacubex.one/en/config/inbound/tun/)

以上外部资料内容均已概括改写，以满足许可合规要求。
