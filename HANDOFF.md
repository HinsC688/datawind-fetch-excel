# 交接文档：DataWind → 飞书 自动化抓取项目

写给完全没有上下文的新对话看。请先整体读完这份文档，再动手操作，避免重复踩坑。

---

## 1. 任务目标

把 DataWind 看板（Push 任务明细表）里的数据，定期同步到飞书的两个表格 tab：

1. **周度数据汇总**：每周更新一批指定任务的周度指标。
2. **日纬度数据**（尚未开始）：更新"上周"的每日明细数据。

因为业务流程和网络环境限制，这不是一次性任务，而是**每周要重复执行**的流程。目标是先手动跑通一次完整链路，再考虑自动化排程。

### 需要抓取的指标（针对每个 task_name）
- 曝光用户数的日均（含环比）
- 点击用户数的日均（含环比）
- 曝光点击率（含环比）
- 注册转化用户数（含环比）
- 注册转化率（含环比）

### 飞书目标位置
- 文档：`PUSH 數據分析和優化` 副本
  - Wiki 链接：`https://l7jipx1bfq.larksuite.com/wiki/Ah29w0uDOicRpCkCtXVus268szd`
  - **spreadsheet_token: `AmzgsAmgNheVVGtcEHQuh2FYsic`**（这是最终确认的写入目标，不是原始表）
- 周度 tab：`周度数据汇总（0803-0906）`
  - **sheet_id: `QUgpS2`**
  - 列结构（A1:H1）：`流程名称,周,任务名称,曝光日均,点击用户日均,曝光点击率,注册转化率,日均注册人数`
  - 每个任务占 6 行模板（1 行数据 + 5 行同任务不同周 + 1 空行分隔），任务名称写在 C 列，流程名称只在每组第一行的 A 列
- 原始参照 tab（不要动）：`周度数据汇总（0629-0802）`，sheet_id `wrsQiu`，在同一个原始文档里（token `WuBQstPzDhWRG7taBotuR6besmf`），不是本次写入目标

### DataWind 看板信息
- 目标看板 URL：`https://datawind.xiaoxiame.com/bi/pages/dashboard/41204?appId=2&sheetId=15473`
- 看板名称：**触达业务价值转化**
- 目标组件：Push 任务明细表，`reportId=286219`
- 真实数据接口：`POST /bi/aeolus/vqs/api/v2/vizQuery/query`
- 关键字段（DataWind 内部字段名，不是中文名）：
  - `task_name`、`workflow_name`、`workflow_id`、`node_id`、`push_type`、`p_date`
  - 指标字段的中文名和 ID 映射保存在响应的 `vizData.aliasMap` 里，每次抓取都要重新读取，不能硬编码 ID（不同请求里同名指标的 ID 可能不同，见坑 #7）

---

## 2. 目前已完成的进度

1. **GitHub 仓库**已建好并连接：`https://github.com/HinsC688/datawind-fetch-excel.git`，本地已配置自动 commit（见坑 #1）。
2. **飞书 lark-cli** 已安装（用 `npx --yes @larksuite/cli@latest ...` 调用，不需要全局安装），App 已配置，用户 `Avery B` 已登录授权，可以读写飞书表格。
3. 已验证目标飞书副本文档可访问、可读、可写。
4. 已确认 DataWind 的真实查询接口、字段结构、`task_name` 与飞书任务名称的匹配关系。
5. **W31（2026-08-03~08-09）周度数据已全部写入**飞书 `周度数据汇总（0803-0906）` tab，26 行模板全部补齐（行号：2,8,14,20,26,32,38,44,50,56,62,68,74,80,86,92,98,104,110,116,122,128,134,140,146,152），已回读验证通过。
   - 第 38、44 行一开始因为 DataWind 首次抓取被 1000 行上限截断而匹配不到，后来单独筛选该流程重新抓取后补齐，详见坑 #13。
6. 相关脚本已写好并验证可用：
   - `scripts/open-chrome.sh`：启动带 CDP 调试端口（9222）的独立 Chrome，用于抓包，不影响日常 Chrome。
   - `scripts/capture-datawind-requests.py`：监听 Chrome CDP 网络请求，把结果存到 `artifacts/`（带时间戳文件名，不会互相覆盖）。
   - `scripts/write-weekly-w31.py`：读取抓取结果 + 飞书模板，逐行写入飞书（已验证可用的正式写入方式，见坑 #6）。

### 2026-08-19 更新：旧 Push 周度汇总的固定周期与新目标

- 当前写入目标已迁移到飞书文档 `注册前触达数据`：`spreadsheet_token=ZJjisFG1bhF8iEtVjMfuIcPXsjf`，tab `push数据`（`sheet_id=BHIztA`）；`彈窗數據` tab 不属于旧 Push 周度项目，绝不改动。
- 周期统一为**周三至下周二，共 7 天**。首次校正已完成：原 W31 行 `2,8,...,152` 已覆盖为 `2026-W31(08/05~08/11)`，同组下一行 `3,9,...,153` 已写入 `2026-W32(08/12~08/18)`。
- 后续周期优先使用 `scripts/fetch-push-weekly.py` 在**已登录的 CDP Chrome 浏览器会话内**直接重放查询；脚本会强制写入绝对 `p_date`、`push_type=apppush` 和 `UJ生命周期=未注册`，不需要在 DataWind 界面逐项筛选。若直接重放不可用，才回退到旧监听方式；监听必须固定 `--seconds 120`，且**必须先启动监听、再由用户修改任何筛选条件**。此规则仅适用于本旧 Push 流程，KYC 周度同步 hook 不作修改。
- 每次 DataWind 抓取**之前**必须读取同一工作簿的 `策略变更-push` tab（`sheet_id=4ppXK3`），按本期周标签在 A/B 列找到策略记录；缺记录、空状态或无法解析变更文本时，停止抓取。运行 `scripts/prepare-push-targets.py` 输出并让用户确认最终的“流程名称 → 任务名称”清单后才能抓取。`本周策略无变更` 维持当前清单；有变更时从清单剔除下线任务、加入上线任务。
- `scripts/validate-push-weekly.py` 和 `scripts/write-push-weekly.py` 均可接收策略计划 `--targets`：下线任务不会再被校验或写入。上线任务会被校验；如果 `push数据` 尚无该任务的格式化六行区块，写入器会停止并列出缺失区块，必须先由用户补充模板，禁止静默漏写。
- `scripts/compare-push-weekly.py` 用于回归测试，逐项比较 26 个目标 × 5 个指标。`2026-08-12~18` 的浏览器重放已与人工抓取 **130/130 个值完全一致**，可作为后续自动重放的基线验证。写入器默认仅 dry-run，`--apply` 才写飞书；无需再手动传 `--row-offset`，它会从每组六行模板中自动定位第一个 B 列及 D:H 均为空的周次行。
- **新坑：任务名称 C 列在每组六行中是纵向合并单元格。** 写第一行（如第 2 行）可写 `B:H`；写后续周次（如第 3 行）只能分别写 `B` 和 `D:H`，禁止向合并区的 `C3` 等非左上角单元格写入，否则飞书会拒绝请求。

---

## 3. 尚未解决的问题（新会话需要先处理）

### 问题 A：日纬度 tab 还没做
用户要的第二部分需求——"更新上周的日纬度数据到日纬度表格 tab"——完全没开始。当前连"日纬度 tab"都还没创建。需要重新跟用户确认：
- tab 名称、列结构（用户之前提供的旧文档模板里有参考格式，是"流程名称,任务名称,日期,曝光/收到,点击/开信,点击率/开信率,FTD,EFTD..."这种结构，但这次的实际需求还没细化）
- 具体要不要参考 `PUSH 數據分析和優化` 副本里的其他 tab 结构

### 问题 B：抓取范围要不要扩展到 W32 之后
本次只做了 W31。后续每周怎么运作、要不要做成自动化脚本一键跑完"抓取+写入"，还没定。

---

## 4. 关键操作流程（照做即可）

### 抓取 DataWind 数据（必须在 FortiVPN 环境下）
```bash
cd "/Users/newair/Desktop/datawind fetch excel"
bash scripts/open-chrome.sh   # 打开独立CDP Chrome，自动导航到目标看板
```
在弹出的 Chrome 登录 DataWind 并进入 Push 任务明细表即可；**不要在启动监听前修改任何筛选条件**。

开新终端窗口，**先启动监听**：
```bash
cd "/Users/newair/Desktop/datawind fetch excel"
python3 scripts/capture-datawind-requests.py \
  --dashboard-id 41204 --no-reload --seconds 120 \
  --prefix push-YYYYMMDD-YYYYMMDD
```
只有终端显示 `Listening now...` 后，才回到 Chrome 设置目标周的 `p_date` 绝对日期范围（不能用"最近7天"等相对筛选）。**DataWind 每一次筛选操作都会自动刷新并发起查询**，因此监听必须先于所有筛选；不要预先设置好日期再启动监听，也不必额外点击刷新。等待脚本结束后，保存的时间戳/前缀响应文件就是本次抓取结果。

结果会保存为：
```
artifacts/{时间戳}-network-events.json
artifacts/{时间戳}-response-bodies.json
```

### 写入飞书（必须在 Clash 环境下，见坑 #2）
先读取当前飞书 tab 模板（确认任务清单/行号，因为用户可能随时改动）：
```bash
npx --yes @larksuite/cli@latest sheets +csv-get \
  --spreadsheet-token "AmzgsAmgNheVVGtcEHQuh2FYsic" \
  --sheet-id "QUgpS2" --range "A1:H339" \
  --output-path "artifacts/weekly-template-latest.json"
```

然后用 `scripts/write-weekly-w31.py` 作为模板改写新脚本（这个脚本名字里带 W31 是历史遗留，实际逻辑是通用的：读模板 → 匹配 DataWind 响应 → 逐行写入）。执行示例：
```bash
python3 scripts/write-weekly-w31.py \
  --template artifacts/weekly-template-latest.json \
  --response artifacts/{时间戳}-response-bodies.json \
  --spreadsheet-token "AmzgsAmgNheVVGtcEHQuh2FYsic" \
  --sheet-id "QUgpS2" \
  --apply
```
不带 `--apply` 时是 dry-run，不会真正写入，**每次正式写入前务必先跑一次 dry-run 确认**。

---

## 5. 踩过的坑，新会话绝对不要重复踩

### 坑 #1：FortiVPN 和 Clash 不能同时连接
这是本项目最大的环境限制。DataWind 是公司内网（走 FortiVPN），飞书/GitHub 走的是 Clash 代理。两者互斥，切换网络时 Kiro 对话本身也可能断线。**解决方案**：把流程拆成两段，FortiVPN 下只做"抓取并存本地 JSON"，切回 Clash 后再"读本地 JSON 写飞书"，两段之间互不依赖网络状态。永远不要设计成"一个脚本里同时抓取和写入"，会在网络切换时失败。

### 坑 #2：自动 Git commit hook 在 FortiVPN 下会报错，这是正常现象
项目配置了 `agentStop` 触发的自动 commit+push hook（见 `.kiro/hooks/auto-commit-changes.kiro.hook`）。已经修复为：GitHub push 失败时不会导致 hook 报错，只会在本地 commit 成功后提示"稍后同步"。如果看到类似 `LibreSSL SSL_connect... unable to access github.com` 的报错，**不要慌，本地提交通常已经成功**，恢复 Clash 后下次 hook 触发会自动补推。

### 坑 #3：Shell 脚本里中文全角符号紧跟变量名会报错
`scripts/open-chrome.sh` 曾经因为 `$PORT）`（全角右括号紧贴变量名）导致 bash 把 `）` 当成变量名一部分，报 `unbound variable`。**教训**：shell 脚本里变量后面如果紧跟非空格字符（尤其是中文标点），一定要用 `${VAR}` 显式加大括号，不能直接写 `$VAR）`。

### 坑 #4：DataWind 的周筛选不要用相对时间
第一次抓取时看板用的是"最近7天"这种相对筛选（`op: lastSync`），抓到的是本周未完整结束的 W32，不能用。必须在 DataWind 里手动切换成**绝对日期范围**筛选（`op: between`），才能抓到完整、稳定的历史周数据。判断方法：抓取后检查请求体里 `query.whereList` 的 `op` 字段，`lastSync` 是相对时间（危险），`between` 才是绝对时间（安全）。

### 坑 #5：DataWind 单次查询有 1000 行上限，返回里会提示真实总量
第一次抓取时 `limit=1000`，但响应里 `atLeast: 4455` 说明实际有 4455+ 条记录，返回被截断了。**如果要抓全量数据（而不是按飞书模板里几十个指定任务筛选），必须处理分页/翻页，不能默认第一页就是全部**。本项目目前的做法是：飞书模板本身只追踪约20个指定任务（不是全部Push任务），所以从截断的1000条里做精确匹配是够用的，但如果以后需求变成"抓取全部任务"，这里要重新设计分页逻辑。

### 坑 #6：飞书 CLI 的批量写入 `--writes` 在本机环境会被 SIGKILL 杀掉
无论是一次性提交48个写入区域，还是分批8个一组提交，`npx @larksuite/cli sheets +cells-set --writes '[...]'` 都会在提交阶段被系统杀死进程（`Signals.SIGKILL`），原因未查明（可能是长命令行参数在某些系统上触发限制）。**每次被杀之前都要先回读验证是否已经部分写入**（本项目验证结果是：SIGKILL发生时飞书没有收到任何写入，是安全失败）。

**唯一验证有效的写入方式**：改成单行单次调用 `+cells-set --range "B{row}:H{row}" --cells '[[...]]'`（不用 `--writes` 批量参数，一次只写一行的 B 到 H 列），这样每次调用参数量小，全部24行逐次调用均成功。`scripts/write-weekly-w31.py` 已经改成这种逐行写入模式，**新会话不要改回批量 `--writes` 模式**，除非先在小范围重新验证过。

### 坑 #7：DataWind 指标的字段 ID 不是固定的，必须用别名表动态查找
一开始尝试直接硬编码字段 ID（比如 `1675528`、`avgday_1675528_1675586`）来取值，后来发现同一个指标"点击用户数的日均"在不同请求返回里，字段 ID 有时对应到 `avgday_1675528_1675585`，中文名字符串里还可能夹杂不可见空格导致按名称匹配失败（`点击用户数的 日均` vs `点击用户数的日均`）。**正确做法**：每次都从当次响应的 `vizData.aliasMap`（字段ID→中文名）动态反查，绝不要跨请求复用硬编码的字段 ID 或依赖中文字符串精确匹配。`scripts/write-weekly-w31.py` 里的 `field_id = {...}` 就是这样动态构建的，参考它的写法。

### 坑 #8：本地临时 JSON 文件容易在工具间产生冲突和损坏
之前尝试手写大段 JSON 到 `artifacts/w31-lark-writes.json`，先后遇到：文件被写入了不相关的乱码文本（怀疑是工具调用参数拼接错误）、编辑器和写入工具对同一文件产生"内容较新"冲突、文件写入后变成 0 字节。**教训**：不要用 fs_write/fs_append 拼接大段手写 JSON 作为中间产物，改成用 Python 脚本在内存里直接构建请求并直接调用 CLI（即 `write-weekly-w31.py` 的做法），完全不落地这种大 JSON 文件，从根本上避免这类冲突。

### 坑 #9：飞书新建 tab 用"复制"而不是"新建空表"
新 tab `周度数据汇总（0803-0906）` 是用 `sheets +sheet-copy` 复制旧 tab `周度数据汇总（0629-0802）` 得到的（保留列结构、格式、合并单元格），然后用 `+cells-clear --scope content` 只清空内容、保留格式。**不要用新建空表再手动搭格式**，容易和原表结构不一致导致后续写入错位。

### 坑 #10：不要把 DataWind 返回的全部任务/流程都写进飞书
DataWind 一次返回上千条各种任务的数据，但飞书模板只想追踪其中一小部分（大约20个 task_name，由用户在飞书表格里手动维护，跟着模板走）。**永远以飞书 tab 里当前已经填写的"流程名称+任务名称"这两列作为唯一的追踪范围来源**，去 DataWind 结果里做精确匹配，不要反过来用 DataWind 的全量结果去决定要写哪些任务。用户在对话里明确说过"以这个tab的流程名称和任务名称为准"。

### 坑 #11：同一个 task_name 可能对应多个 workflow_name，这是正常的业务逻辑
不要把它当成数据异常去"去重"或"合并"。必须用 `流程名称(workflow_name) + 任务名称(task_name)` 两个字段一起作为唯一键去匹配，单独用 task_name 匹配会匹配到多条不该合并的记录。

### 坑 #13：DataWind 单次查询的 1000 行截断，可能刚好把你要的任务挤出去
第一次抓 W31 全量数据时，模板里第38、44行（流程"中台未注册-非首次-Airdrop-0803-延时90s/2min-App"下的 `-0805-` 版本任务）用"流程名称+任务名称"精确匹配始终匹配不到，一度误判为"模板任务名称跟 DataWind 不一致，需要用户确认改名"。后来重新抓取时，让用户在 DataWind 页面里先筛选/定位到这个具体流程再刷新（而不是整表刷新），得到的响应里就出现了这两条 `-0805-` 记录（同时还看到了 `-0731-` 的旧版本记录）。

**结论**：如果某个任务在全量抓取结果里找不到，先别急着断定"模板名称有误"或"永久留空"，要怀疑是不是被 1000 行截断挤掉了。正确排查方法：在 DataWind 界面上按该流程筛选（或搜索该任务名称）后单独刷新抓取一次，响应条数会明显变少（比如从1000+条变成4-5条），此时基本能覆盖到目标数据。**用户在 DataWind 界面上实际看到的数据，才是最终判断依据**，抓取结果和界面不一致时，优先怀疑抓取被截断，而不是怀疑界面显示错误。

### 坑 #14：用户发的截图链接可能是过期的教程示例，不要重复使用
对话中用户多次发送一张 `open.feishu.cn/page/cli?user_code=HPP7-2C5D...` 的截图。经确认这是 lark-cli 官方文档/教程里的示例截图（配置码格式说明），不是本项目当前有效的授权链接。**当前项目真实使用的 App ID 和登录状态已经配置完成**（见下方"当前授权状态"），如果新会话又看到类似截图，先确认是不是文档示例，不要盲目重新走一遍配置流程。

---

## 6. 当前授权状态（供检查用，不要重新配置除非失效）

- 飞书 App ID: `cli_aafb0047f1385db8`
- 登录身份: 用户 `Avery B`（`ou_5fbd8bf917e8fb7f9aa59a04b1c1219f`）
- Token 有效期: 到 2026-08-13（refresh 到 2026-08-20）——**新会话时间是否已经过期需要先检查**，如果过期需要重新跑：
  ```bash
  npx --yes @larksuite/cli@latest auth status
  # 如果失效：
  npx --yes @larksuite/cli@latest auth login --recommend
  ```
- GitHub 远程: `https://github.com/HinsC688/datawind-fetch-excel.git`，分支 `main`，已有自动 commit hook。

---

## 7A. 新任务进度（2026-08-18 开始）：KYC 整體觸達數據

### 新项目信息（跟第1-6节的 PUSH 周度汇总项目是两个不同的项目，不要混淆）
- 飞书文档：`KYC 整體觸達數據`
  - Wiki 链接：`https://l7jipx1bfq.larksuite.com/wiki/ENypwi8abiNuUjk6SzHuWx22slh`
  - **spreadsheet_token（obj_token）: `Wmq4s0mJHh3HZ7tITvbu261ZsFb`**
  - 4个 tab：
    - `彈窗數據`：sheet_id `b45c2f`，列 A1:M1 = `Week,触达工具,触达详情,弹窗模版,注册距今时间范围,uj_type,是否kyc,曝光用户,点击用户,点击率,触达24h内kyc,24h内kyc率,備註`。当前只track APP渠道模板（"中台-D1至0409后注册用户KYC引导-弹窗A/B组50%分流-APP..."这类），**忽略WEB/H5部分**。目前数据只写到 `0706-0714` 周期（第32-39行），历史断档大（`0709-0716`~`0805-0811`都没补），**这次只加 `0812-0817` 一组即可，不用回补历史**。
    - `邮件数据`：sheet_id `6kyjFR`，列 A1:P1 = `周期,流程名称,reg_range,uj_type,is_kyc_show,曝光用户,点击用户,点击率,触达24h内FTD,24h内FTD率,触达24h内eFTD,24h内eFTD率,触达24h内eFTTc,24h内eFTTc率,触达24h内kyc,24h内kyc率`。已写到 `0805-0811`（第90-110行，110行是最后一行，111行需确认是否空行分隔）。**下一批 `0812-0817` 从112行左右开始写（先读一遍A108:Q115确认空行位置再写）**。
    - `push数据`：sheet_id `BHIztA`，列 A1:J1 = `周期,流程名称,reg_range,uj_type,is_kyc_show,曝光用户,点击用户,点击率,触达24h内kyc,24h内kyc率`。已写到 `0805-0811`（第94-115行）。**下一批 `0812-0817` 从116行左右开始写（116空行分隔，117起新数据，先读A113:J120确认）**。
    - `KYC 觸達 AP 連結`：sheet_id `6fQBnz`，纯参考链接表，不涉及本次写入。
  - 三个数据tab都是"周期分组+多行任务"结构，同一周期只在该组第一行A列写周期标签，其余留空（跟旧项目"流程名称"列的填法逻辑一致，但这里A列是"周期"不是"流程名称"）。
  - **用户确认**：抓取数据以"上周"（即 `0812-0817`）DataWind界面上实际显示的流程名称为准，不要用飞书历史行反推。

### DataWind 新看板信息
- 目标看板 URL：`https://datawind.xiaoxiame.com/bi/pages/dashboard/40450?appId=2&sheetId=14003`
- dashboard_id: `40450`（跟旧项目的 `41204` 不是同一个看板，抓取脚本需要传 `--dashboard-id 40450`）
- 具体的组件/reportId、真实查询接口字段结构，**还没抓取验证过**，需要在FortiVPN环境下打开新看板后重新确认（大概率还是走 `POST /bi/aeolus/vqs/api/v2/vizQuery/query`，但reportId等要重新读取响应确认，不能直接套用旧项目的 `286219`）。

### 脚本改动（已完成）
- `scripts/capture-datawind-requests.py` 已支持 `--dashboard-id` 参数（默认还是旧项目的 `41204`，兼容旧用法），新项目需要显式传 `--dashboard-id 40450`。同时加了 `--prefix` 参数，可选自定义文件名前缀。

### 抓取策略（已与用户确认）
- **目标组件：看板里的"触达详情数据"板块**（看板标题实际显示为"入金转化监控"，dashboard 40450）。
- **必须在 DataWind 界面上取消 `p_date` 维度**，否则表格是"每天一行"，而飞书三个tab都是"整周期聚合成一行"的粒度，粒度不匹配。取消p_date后表格变成按流程名称聚合。
- **弹窗、邮件、push 三类渠道一次性全量抓取，不在DataWind界面按渠道分三次筛选**，抓回来后在本地按渠道字段拆分成三份，分别写入对应tab。用户已确认这样操作更省事。
- 日期筛选：绝对日期 `2026-08-12` ~ `2026-08-17`（用户已在界面设好）。
- **全量抓取要特别警惕1000行截断**（坑#5/#13）：取消p_date后行数会大幅减少（不再按天膨胀），理论上不容易触发上限，但抓完后**必须检查响应里的 `atLeast`/`total`/`hasMore` 字段**确认没被截断。如果被截断，改成按渠道分3次筛选抓取。

### 抓取失败记录（2026-08-18 第一次尝试）
`artifacts/20260818T160852-*.json` 这一对文件是**无效抓取**，里面只有2个 `dashboardReview/reviewUpdate` 埋点请求，没有任何 `vizQuery/query` 数据请求。原因：用户在**启动抓取脚本之前**就已经设好筛选条件并完成了查询，监听窗口内表格没有重新发起查询。

**教训（新坑）**：抓取的正确顺序必须是"先启动监听脚本 → 再去浏览器操作触发查询"，不能反过来。如果筛选条件已经设好了，光点"刷新"按钮可能命中缓存不发请求，此时应该用"改一下筛选再改回来"或者 `Cmd+R` 硬刷新整个页面来强制触发真实查询。抓完后**第一件事就是检查响应体里有没有 `vizQuery/query` 这个URL**，没有就是白抓了，别急着往下分析。

### ✅ 0812-0817 已完成（2026-08-18）
三个 tab 共 53 行全部写入并回读验证通过：
- `push数据`（BHIztA）第 117-138 行，22 行
- `邮件数据`（6kyjFR）第 112-132 行，21 行
- `彈窗數據`（b45c2f）第 83-92 行，10 行（写入前先用 `+dim-insert` 在第81行后插入11行，把 Web/H5 区块从83行下移到94行）

复用脚本：
- `config/touch-details-0812-0817.txt`：DataWind"触达详情"筛选清单（35个去重名称）
- `config/write-plan-0812-0817.json`：写入方案（每行的流程名称+档位+文本列）
- `scripts/write-kyc-weekly.py`：读抓取结果→匹配→逐行写入，默认dry-run，加 `--apply` 才写
- `scripts/check-capture.py`、`scripts/analyze-coverage.py`、`scripts/list-touch-details.py`：抓取后的校验和排查工具

### 本次新增的坑（下个周期直接照做，别重犯）

#### 坑 #15：必须先启动监听脚本，再去浏览器操作触发查询
第一次抓取（`20260818T160852`）只抓到2个埋点请求、0个 `vizQuery/query`，因为筛选是在启动脚本**之前**设好的，监听窗口内表格没重新查询。光点"刷新"按钮可能命中缓存不发请求。正确做法：先跑监听脚本，看到 `Listening now` 后再去改筛选/取消维度/`Cmd+R` 硬刷新。抓完第一件事就是跑 `check-capture.py` 确认有没有 `vizQuery/query`，没有就是白抓。

#### 坑 #16：用户每改一次筛选都会产生一个请求，必须挑出"筛选状态最终态"的那个
一次抓取里通常有多个 `vizQuery/query`（本次10个），它们是筛选的渐进状态。**不能按行数最多来挑**——行数多往往是因为筛选还没加全。

#### 坑 #17【最关键】筛选渐进过程中的请求可能多带一个未命名拆分维度，导致取数取到碎片值
本次 `20260818T165256` 里有5个请求都满足"带detail筛选+绝对日期+未截断+无p_date维度"，但其中4个（254/914/580/432行）多带了一个未命名维度 `1677704`，使同一个 `(触达工具,触达详情,reg_range)` 被拆成多行。按唯一键建索引时只会保留最后一条，取到的是碎片值（表现为曝光用户=1、点击用户=0 这种明显不对的小数字）。

**判据：要求 `(触达工具, 触达详情, reg_range)` 唯一键在该请求内不重复。** 重复率会很触目（914行里828处重复）。正确的那个请求本次是78行，刚好等于 push 30 + 邮件 29 + 弹窗 19。`write-kyc-weekly.py` 的 `pick_query()` 已内置这个检查，会打印跳过原因。

#### 坑 #18：读飞书表格不能只信 current_region，空行之后往往还有数据
`+csv-get` 返回的 `current_region` 只是从A1开始的连续区域，遇到空行就截断。本次两次踩坑：
- 邮件 tab 以为只写到 `0715-0721`（第44行），实际第46-110行还有三个周期的数据
- 弹窗 tab 以为只写到 `0706-0714`（第39行），实际第41行有"App"标题、42-81行是App数据，第83行有"Web/H5"标题、84-113行是Web数据

**做法：读到底（比如 A1:P200），或者按 `row_count` 读全表，看到空行不要停。** 工具返回的 warning_message 里其实已经提示了"数据没读全"，要认真看。

#### 坑 #19：往表格格式区域之外的空行写入，不会自动继承数字格式
push/邮件 tab 直接往第117/112行起的空行写，百分比显示成 `0.02483225` 而不是 `2.48%`，整数也没有千位分隔符。而弹窗 tab 用 `+dim-insert --inherit-style before` 插入的行**格式完美继承**，一次到位。

两个解法：
1. **推荐**：写入前用 `+dim-insert --position <行号> --count <行数> --inherit-style before` 在最后一行数据之后插入行（`--inherit-style before` 会继承前一行即数据行的样式），再往新行写。
2. 补救：写完后用 `+cells-set-style --number-format` 逐列补格式，整数列 `#,##0`、百分比列 `0.00%`。注意这个命令**不支持一次传多个不连续区域**，每列要单独调用一次。

注意 `+dim-insert` 的参数是 `--position` 和 `--count`（不是 `--major-dimension/--start-index/--end-index`），`--position N` 表示插在第N行**之前**。

#### 坑 #20：百分比列写小数，不要写百分数字符串
点击率写 `0.0248` 而不是 `"2.48%"`，靠单元格的百分比格式渲染。写字符串会变成文本，无法参与计算；写 `2.48` 会显示成 `248%`。

#### 坑 #21：`+cells-clear` 是 high-risk-write，需要 `--yes`
本次想清空重写时遇到，最后改用非破坏性的补格式方案绕过了。如果确实要清，记得加 `--yes`，并且清之前确认清的是自己刚写的范围。

### ⚠️ 周期定义（用户2026-08-18确认）：周三 → 下周二，固定7天
标签格式 `MMDD-MMDD`。在**周三**运行，同步刚刚结束的那一期（上周三 ~ 昨天周二）。

历史各期核对结果：
| 标签 | 实际日期 | 星期 | 天数 |
|---|---|---|---|
| 0715-0721 | 07-15 ~ 07-21 | 周三→周二 | 7 ✅ |
| 0722-0728 | 07-22 ~ 07-28 | 周三→周二 | 7 ✅ |
| 0729-0804 | 07-29 ~ 08-04 | 周三→周二 | 7 ✅ |
| 0805-0811 | 08-05 ~ 08-11 | 周三→周二 | 7 ✅ |
| **0812-0817** | 08-12 ~ 08-17 | 周三→**周一** | **6 ⚠️** |

**原因**：`0812-0817` 是在**周二 2026-08-18** 跑的（纯属测试，比正常的周三提前一天），当天数据不完整所以只截到 08-17，导致这期只有6天。累计指标（曝光/点击/注册数）比7天口径低约14%，跟上下期做环比会有偏差。

---

## ✅ 已完成事项（2026-08-19 周三运营）

### 上次尝试记录：2026-08-18（周二）触发了 hook，但在第0步就停住了，**没有抓取、没有写入**

那次运行只做了只读检查，飞书内容一个字都没改。停下来的原因是第0步校验不通过：
- 运行日是**周二**，不是周三。按"周三→周二"规则算出的"刚结束的一期"是 08-12(周三) ~ 08-17(**周一**)，只有 6 天，正是 `0812-0817` 这期当初出错的同一个原因。
- 而待办要的 `0812-0818` 里 08-18 就是那天**当天**，违反"绝不抓包含今天的范围"。

顺带确认了三件事（下次可以不用重复查）：
- 飞书 CLI 登录有效，用户 `Avery B`，refresh token 到 **2026-08-25**（access token 每天会自动续，不用管）。
- `detect-write-rows.py` 读出三个 tab 最后一组周期标签都是 `0812-0817`，最后一行数据分别是 push **138** / 邮件 **132** / 弹窗 **92**（弹窗 Web/H5 标题现在在第 **94** 行）。跟下面记的写入范围 push 117-138、邮件 112-132、弹窗 83-92 完全吻合，说明 08-18 那轮写入是完整的、没有残缺。

**教训**：hook 的第0步校验是有用的，不要因为"就差一天"而将就跑。宁可推到周三。

### ✅ 已完成：将 `0812-0817` 覆盖为 `0812-0818`

**说明**：这不是额外一期，而是周三 2026-08-19 应同步的完整周三→周二周期（08-12 至 08-18）。

**2026-08-19 最终执行结果**：DataWind 数据上传后重放绝对日期查询，使用 `artifacts/20260819T125125-response-bodies.json`（78 行；无 `p_date` 维度；未触及 `limit=5000`；唯一键无重复；53 个目标行全部匹配）。相较原 6 天版本，累计数据出现合理的 08-18 增量：push 曝光/点击/kyc +13,365/+339/+57，邮件曝光/点击/kyc +33,570/+2,256/+448，弹窗曝光/点击/kyc +5,342/+2,476/+963。已直接覆盖、未插入行：push（BHIztA）117-138、邮件（6kyjFR）112-132、弹窗 App（b45c2f）83-92。三张表均回读验证：A 列标签为 `0812-0818`、百分比和整数格式正确，且弹窗 Web/H5 标题第 94 行完好。

**执行要点（跟常规流程的唯一差别：覆盖而非追加）**：
- 取数：`python3 scripts/fetch-datawind.py --start 2026-08-12 --end 2026-08-18 --details config/touch-details-0812-0817.txt`
- 周期标签改成 `0812-0818`（改 `config/write-plan-*.json` 的 `period_label`）
- **直接覆盖已写入的行，不要插入新行、不要往下追加**：
  - `push数据`（BHIztA）第 **117-138** 行
  - `邮件数据`（6kyjFR）第 **112-132** 行
  - `彈窗數據`（b45c2f）第 **83-92** 行
- 这些行已经有正确的数字格式了（08-18 那轮已补过），所以**不需要再跑 `+dim-insert`，也不需要补 number-format**
- 名单、行号、写入方案都不用改，只改日期范围和 `period_label`
- 跑法：`python3 scripts/write-kyc-weekly.py --tab push --start-row 117`（先dry-run）→ 加 `--apply`；邮件 `--start-row 112`；弹窗 `--start-row 83`
- 写完回读验证，确认 A 列标签变成 `0812-0818`、数值比 6 天版本略高（多了周二一天）

- ⚠️ **不要照抄 `detect-write-rows.py` 打印的建议行号。** 那个脚本永远假设你要"追加新一期"，所以这次会建议 push 插在 139 行、邮件 133 行、弹窗 93 行（数据起始 140/134/94）。**照它做会多出一期重复数据**，把 `0812-0818` 追加在 `0812-0817` 后面，而不是覆盖它。这次要的起始行是 **117 / 112 / 83**。该脚本这次只用来确认"最后一组还是 `0812-0817`"，行号建议直接忽略。
- ⚠️ **`config/write-plan-0812-0817.json` 里弹窗那段的行号是过期的**（`app_block_last_row: 81`、`blank_separator_row: 82`、`web_header_row: 83`、`needs_row_insert: true`），那是 08-18 那轮**插入11行之前**的快照，插完就没回写。实际现状是数据 83-92、Web/H5 标题在 94。这次弹窗要用 `--start-row 83`，并且**必须把 `needs_row_insert` 当成 false**（配置里写的 true 是错的，别让它触发插行）。push / 邮件 的 `write_start_row`（117 / 112）是准的。

**再下一期**：`0819-0825`（08-19周三 ~ 08-25周二），要等到 **2026-08-26 周三** 才同步，明天不要做这一期。

**校验规则**（已写进 hook 的第0步）：算出周期后必须满足 天数==7、开始日是周三、结束日是周二；不满足就停下来问用户。**绝不抓包含"今天"的范围**。

### 业务口径确认（用户已明确，下周期沿用）
1. **追踪范围**：以飞书各 tab 里"上一周期"（本次是 0805-0811）已填写的流程名称为准，不要用 DataWind 全量结果反推（坑#10）。
2. **流程名称与档位一一对应**：同一个流程在 DataWind 里可能横跨多个 `reg_range`（比如 `EMAIL-...-下载APP并登录` 五档都有数据），**只取飞书那行写定的那一档，其他档位数据不计入**，不做加总。
3. **`注册90天以上` 归入 `注册30天以上`**：DataWind 的 reg_range 只有五档（注册当天/1-7天/8-14天/15-30天/30天以上），没有90天以上，它被涵盖在"注册30天以上"里。用户已把飞书 push/邮件 tab 里 `90日后-0716` 那行的 reg_range 改成"注册30天以上"。
4. **弹窗只做 App 区块，Web/H5 跳过**。
5. **uj_type / is_kyc_show 两列统一填 `注册未FTD` / `未kyc`**：虽然飞书历史行里有些留空，但抓取数据本身就是这个口径筛出来的，填上更准确。用户已确认。
6. **DataWind 固定筛选**：`uj_type=注册未FTD`、`is_kyc_show=未kyc`、`reg_range` 五档全选、`p_date` 绝对日期、**界面上取消 p_date 维度**（否则按天拆，跟飞书整周期聚合对不上）。

### 下个周期（0819-0825 等）怎么做
1. 读飞书三个 tab 最新一期的流程名称，更新 `config/touch-details-*.txt` 和 `config/write-plan-*.json`（周期标签、写入起始行、名单）。注意弹窗的实验名称几乎每两周就换一轮（0706→0715→0722→0727→0806→0813），必须每次重新核对。
2. 切 FortiVPN，先启动监听脚本，再在界面上按清单筛选"触达详情"、设好其他筛选、取消 p_date 维度。
3. 跑 `check-capture.py` 确认抓到了、没截断、没相对时间。
4. 切回 Clash，先用 `+dim-insert --inherit-style before` 插入带格式的行，再跑 `write-kyc-weekly.py --tab all`（先dry-run，再 `--apply`）。
5. 回读验证数值和格式。

### 当前卡点（2026-08-18，已解决）
用户当前网络环境是 Clash（跟Kiro对话连接用的网络），DataWind内网需要切到FortiVPN才能访问，而**这台机器上FortiVPN和Clash不能同时连接**，用户切换到FortiVPN会导致跟Kiro的对话断线。用户已确认理解这个限制，准备手动切换。

**如果这轮对话在切换网络后断线，新会话/新一轮对话请直接执行以下步骤**（不需要再重新问用户需求，以上确认过的范围直接执行）：
1. 检查网络：`curl -s -o /dev/null -w "%{http_code}\n" --connect-timeout 5 https://datawind.xiaoxiame.com`，返回200/其他非000说明FortiVPN已连通。
2. 打开新看板：`bash scripts/open-chrome.sh "https://datawind.xiaoxiame.com/bi/pages/dashboard/40450?appId=2&sheetId=14003"`
3. 用户在弹出的Chrome里登录DataWind，进入看板，找到本次要抓的几个明细表组件（弹窗/邮件/push三类数据可能是不同组件，也可能在同一看板的不同tab/区域），把时间筛选切换成绝对日期范围 `2026-08-12` 至 `2026-08-17`（**必须绝对日期，不能用"最近7天"等相对筛选**）。
4. 新开终端运行抓取：`python3 scripts/capture-datawind-requests.py --dashboard-id 40450 --no-reload --seconds 120`，看到"Listening now"提示后回Chrome里点刷新。
5. 抓取完成后，网络切回Clash（用户手动操作），验证飞书连通性后，读取三个tab现有内容的**末尾几行**（比如push数据tab读 `A110:J120`，邮件数据tab读 `A105:Q115`，弹窗数据tab读 `A25:M40`）确认准确的空行分隔位置和插入起始行号，然后比照 `scripts/write-weekly-w31.py` 的逐行写入模式（**禁止用批量`--writes`**，见坑#6），为这三个tab各写一版新脚本（或参数化成一个通用脚本），逐行写入 `0812-0817` 的新数据。
6. 写入前必须先dry-run，写入后必须回读验证（见坑#6和SOP第5节）。
7. 字段ID仍需从新看板响应的 `vizData.aliasMap` 动态查找，不要假设跟旧项目一致（见坑#7）。
8. 注意新看板的1000行截断风险，抓取后检查响应里的`atLeast`/`total`类似字段（见坑#5/#13）。

---

## 7. 建议新会话的第一步

1. 检查飞书 CLI 登录状态是否还有效（`auth status`）。
2. 读一遍这份文档全文，尤其是第3节"尚未解决的问题"和第5节的坑。
3. 跟用户确认：
   - 日纬度 tab 的具体需求（列结构、tab 命名规则）；
   - 是否要开始处理 W32 及后续周期，还是继续做日纬度。
4. 不要重新发起飞书 App 配置流程（已完成），除非 `auth status` 显示确实过期。
